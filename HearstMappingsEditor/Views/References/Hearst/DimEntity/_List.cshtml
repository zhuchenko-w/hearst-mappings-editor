@using HearstMappingsEditor.Data.Models

@model IList<DimEntity>

@if (Model.Any())
{
    foreach (var item in Model)
    {
        Html.RenderPartial("_ListItem", item);
    }
}
else
{
    <tr class="nothing-found">
        <td colspan="6">Nothing found</td>
    </tr>
}