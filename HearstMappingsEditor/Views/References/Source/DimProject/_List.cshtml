@using HearstMappingsEditor.Data.Models

@model IList<DimProject>

@if (Model.Any())
{
    foreach (var item in Model)
    {
        Html.RenderPartial("_ListItem", item);
    }
}
else
{
    <tr class="nothing-found">
        <td colspan="14">Nothing found</td>
    </tr>
}