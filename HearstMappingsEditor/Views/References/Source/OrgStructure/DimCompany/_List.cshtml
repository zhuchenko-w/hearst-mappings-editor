@using HearstMappingsEditor.Data.Models

@model IList<DimCompany>

@if (Model.Any())
{
    foreach (var item in Model)
    {
        Html.RenderPartial("_ListItem", item);
    }
}
else
{
    <tr class="nothing-found">
        <td colspan="5">Nothing found</td>
    </tr>
}