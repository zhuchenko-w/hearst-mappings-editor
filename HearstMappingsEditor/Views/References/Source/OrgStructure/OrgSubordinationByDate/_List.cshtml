@using HearstMappingsEditor.Data.Models

@model IList<OrgSubordinationByDateViewModel>

@if (Model.Any())
{
    foreach (var item in Model)
    {
        Html.RenderPartial("_ListItem", item);
    }
}
else
{
    <tr class="nothing-found">
        <td colspan="9">Nothing found</td>
    </tr>
}