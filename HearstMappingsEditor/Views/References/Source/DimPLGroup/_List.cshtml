@using HearstMappingsEditor.Data.Models

@model IList<DimPLGroup>

@if (Model.Any())
{
    foreach (var item in Model)
    {
        Html.RenderPartial("_ListItem", item);
    }
}
else
{
    <tr class="nothing-found">
        <td colspan="3">Nothing found</td>
    </tr>
}